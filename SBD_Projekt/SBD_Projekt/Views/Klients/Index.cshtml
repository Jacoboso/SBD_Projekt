@model IEnumerable<SBD_Projekt.Models.Klient>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.id_osoba)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.id_prawnik)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.id_osoba)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.id_prawnik)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id_klient">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id_klient">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id_klient">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
